/** Repeated app paths — import instead of scattering string literals. */
export enum AppRoute {
  Dashboard = '/',
  Programs = '/programs',
  Calendar = '/calendar',
  Validation = '/validation',
  AiAssist = '/cli',
  Login = '/login',
}
